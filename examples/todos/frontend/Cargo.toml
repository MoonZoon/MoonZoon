[package]
version = "0.1.0"
name = "frontend"
repository = "https://github.com/MoonZoon/MoonZoon"
authors = ["Martin Kavík <martin@kavik.cz>"]
description = "Todos - MoonZoon example"
categories = ["MoonZoon", "example"]
license = "MIT"
readme = "../README.md"
edition = "2018"

[lib]
crate-type = ["cdylib"]

[dev-dependencies]
wasm-bindgen-test = "0.3.19"

[dependencies]
zoon = { path = "../../../crates/zoon" }
strum = { version = "0.21.0",  features = ["derive"], default-features = false }
uuid = { version = "0.8.2", features = ["wasm-bindgen", "v4"], default-features = false }
# getrandom = { version = "0.2", features = ["js"], default-features = false }

[package.metadata.wasm-pack.profile.release]
wasm-opt = ['-Os']
